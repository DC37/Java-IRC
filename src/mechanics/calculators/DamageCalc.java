package mechanics.calculators;

/**
 * Calculates the damage (or HP healed) by
 * a Skill. Found in the Player class.
 * <p>
 * Originally, this would have been a program
 * by itself, but since its integration into
 * Character.exe (at Version 4.00), and since
 * the migration of the Paintasy tracking program
 * onto JAVA, this program has become obsolete as
 * a standalone and is now integrated into both the
 * character tracker and the bot programmes.
 * @see Player
 * @see EXPCalc
 * @see StatsCalc
 * @author RehdBlob
 * @version 0.93a
 * @since 0.9
 * @since 2011.0701
 */
public class DamageCalc
{
	public double compute(int stat1, int stat2) 
	{
		return 0;
	}

}
